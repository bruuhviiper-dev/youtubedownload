[phases.setup]
nixPkgs = ["php82", "composer", "nodejs", "ffmpeg", "curl"]

[phases.build]
cmds = [
    "composer install --no-dev --optimize-autoloader",
    "mkdir -p bin",
    "curl -L https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp -o bin/yt-dlp",
    "chmod a+rx bin/yt-dlp"
]
